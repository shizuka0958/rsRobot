app.controller('welcomeController', ['$scope', '$http', '$rootScope', function ($scope, $http, $rootScope) {

    // $scope.$on('warn query', function (event, data) {
    //     if (data.err == 0) {
    //         $scope.warnList = data.result
    //         $scope.warnCount = $scope.warnList.length;
    //         $scope.$apply();
    //     } else {
    //         $scope.warnList = [];
    //         $scope.warnCount = 0;
    //         $scope.$apply();
    //         showMsgBox('error', data.result)
    //     }
    // });
}])